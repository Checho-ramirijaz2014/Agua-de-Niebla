import subprocess, sys
import xml

def install_toolkit():
    """install the java toolkit"""
    subprocess.run(["echo", "checking if NOAA's Toolkit is already installed..."])
    

def check_toolkit():
    "checks if the toolkit is already installed"

class Toolkit():
    def __init__(self):
        pass